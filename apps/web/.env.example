NEXT_PUBLIC_SUPABASE_URL=https://replace-me.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace_me
# Adresse privée utilisée uniquement par le serveur Next (facultative en local).
SUPABASE_SERVER_URL=
# URL directe GoTrue sur un réseau dédié web↔Auth (facultative en local uniquement).
SUPABASE_AUTH_INTERNAL_URL=
# `openssl rand -base64 32 | tr '+/' '-_' | tr -d '='` ; jamais NEXT_PUBLIC_*.
SUPABASE_AUTH_RATE_LIMIT_SECRET=
API_URL=http://localhost:3001
